# WordPress REST API base URL (no trailing slash)
# When set, /blog and /blog/[slug] fetch live content from WordPress.
# Leave empty to use local markdown content in src/content/posts/.
WORDPRESS_API_URL=https://your-wordpress-site.com
